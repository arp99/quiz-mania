import instuctionIcon from "../assets/instruction.png"

export const InstructionIcon = (props : any) => {
    return (
        <img src={instuctionIcon} alt="instruction icon" style={{...props}} />
    );
}
